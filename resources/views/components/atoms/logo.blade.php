<div class="w-full  flex justify-center items-center">
    <img
        src="{{ asset('images/logo/logo.png') }}"
        alt="{{ trans('Zimou group logo') }}"
        {{$attributes->merge(['class'=>''])}}
    >
</div>
